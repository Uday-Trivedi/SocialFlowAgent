# SocialFlowAgent
SocialFlowAgent is a rule-based, agentic RAG system that answers product queries for a SaaS platform, detects user intent, retrieves relevant context using vector search, and captures business leads. It uses local embeddings, FAISS, and Gemini with simple conditional tool chaining.
